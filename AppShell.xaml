<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GuessWhoGame.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GuessWhoGame.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="GuessWhoGame">

    <!-- Define the LoginPage as the initial route -->
    <ShellContent Title="Login" ContentTemplate="{DataTemplate local:LoginPage}" Route="LoginPage" />

    <!-- Define other pages -->
    <ShellContent Title="Home" ContentTemplate="{DataTemplate local:HomePage}" Route="HomePage" />
    <ShellContent Title="Game" ContentTemplate="{DataTemplate local:GamePage}" Route="GamePage" />
</Shell>
